// import tests.packages {...}
import libc {fopen, fclose}

func dummy(n: int): int {
	return n + 69;
}

func main(argc: int, argv: char**): int {
	file := fopen("test.txt", "r");
	fclose(file);

	n := get_input_int();
	dummy_result := dummy(n);
	// do_some_prints();

	return 0;
}

