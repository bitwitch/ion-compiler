func f1(a: int): int {

	func_ptr1: func(one: int, two: float): int;
	func_ptr2: func(int, float): int;

	str := "fried chicken";
	str2: char*;
	// TODO: this requires pointer decay
	str2 = str;
	str3 := "hey dude";
	str2 = str3;
	print(str3);

	fart: bool = true;
	// wat heppen?
	if (fart) {
		return a;
	} else {
		return a+1;
	}

}

func print(s: char*) {
	
}

func main(argc: int, argv: char**): int {
	f1(69);
	return 0;
}
// dis last ting
